#!name=自动切换出站模式 (WiFi)
#!desc=连接家里 WiFi 自动切直连，断开切回规则。兼容 Surge & Egern。
#!system=ios

[Script]
# 注意：将 argument 里的 "MyHomeWiFi" 替换为你家里真实的 WiFi 名称（严格区分大小写，如果有多个可以用逗号隔开）
WiFi自动切换 = type=event, event-name=network-changed, script-path=https://raw.githubusercontent.com/marrylily/share/refs/heads/main/JavaScript/wifi_switch.js, argument="5Gmin"
